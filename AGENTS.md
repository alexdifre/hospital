# AGENTS.md

## Python environment

For this repository, never use plain `python` for project tasks.

Always run Python commands with this exact interpreter wrapper:

```bash
conda run -n mlc-stack python